package com.jimbarritt.spikes.parsejava.example.symbiosis;

public interface PollenProvider {

    /**
     * 
     * @todo A todo statement as a javadoc
     */
    void accept(Pollinator pollinator);
}
