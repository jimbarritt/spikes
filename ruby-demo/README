A spike which demonstrates some environmental and language features in ruby.

You need the following gems installed:

All this is done for you if you run ./install.rb

gem install gherkin
sudo gem install cucumber
gem install rspec

This guy shows how to get the whole setup running
http://gamutworks.net/posts/4

sudo gem install ZenTest
sudo gem install hoe
sudo gem install redgreen

