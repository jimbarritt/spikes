/**
 * This template uses some sub templates which are defined in simpleGroup.stg
 * In order to use it you have to set the supergroup of the template group which loads this one.
 */


$input:outerTemplate()$

$input:toCSVRow()$